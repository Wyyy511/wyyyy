# V7 视觉刷新说明

本版只重做前端视觉与交互呈现，不改 Baseline、Scenario、数据查询、DeepSeek API 和报告导出逻辑。

## 视觉方向
- 参考用户提供的 DeepSeek 首页：浅蓝网格背景、大标题、居中的大输入框、圆形发送按钮、弱化技术元素。
- 保留 WaterPulse 自身水风险定位，使用蓝色 + 青绿色而非照搬品牌视觉。
- 初始页为 Landing 状态；发送第一条消息后自动切换为安静的聊天工作区。

## 交互优化
- 快捷分析入口改为 3 个轻量卡片。
- 上传、Excel 模板、语音入口集中到输入框内部。
- 后台 Trace 仍默认折叠。
- 修复快捷分析按钮原先 class 不匹配导致无法点击的问题（`.suggestion` -> `.quick`）。
